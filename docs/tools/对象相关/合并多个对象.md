# 合并多个对象

## 01-概述

- 合并多个对象，返回一个合并后的对象（不改变原对象）

## 02-代码实现

```js
export function mergeObj(...objs){
    const result = {}
    objs.forEach(obj=>{
        Object.keys(obj).forEach(key=>{
            if(!result.hasOwnProperty(key)){
                result[key] = obj[key]
            }else{
                result[key] = [].concat(result[key],obj[key])
            }
        })
    })
}
```

